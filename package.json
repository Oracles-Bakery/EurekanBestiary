{
  "scripts": {
    "prestart": "cp node_modules/purecss/build/pure-min.css public/pure.css && cp node_modules/purecss/build/grids-responsive-min.css public/pure-responsive-grid.css",
    "prebuild": "npm run prestart",
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.7.0",
    "snowpack": "^3.3.7"
  },
  "dependencies": {
    "dayjs": "^1.10.6",
    "fast-csv": "^4.3.6",
    "minimist": "^1.2.5",
    "purecss": "^2.0.6",
    "svelte": "^3.42.1"
  }
}
